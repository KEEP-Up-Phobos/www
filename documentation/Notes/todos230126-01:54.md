# 📋 KEEP-Up Todo — January 23, 2026 @ 01:54

This is today's working log. Below are the outstanding (incomplete) todos carried forward from the main session notes. Each item includes the immediate next steps to make progress.

---

## Outstanding Tasks (Jan 23, 2026)

1. Fix Auth Session Handoff Bugs
   - Problem: session loss across refresh/navigation and cross-port exchanges.
   - Next steps:
     - Add debug logging to `main_server.js` session validation.
     - Ensure cookie attributes (`SameSite`, `Secure`, `Domain`) set appropriately for local/dev flows.
     - Confirm `credentials: 'include'` in frontend fetches (`/frontend/react-keepup/src/api/*`).
     - Add a session refresh endpoint and client-side refresh before expiry.
   - Priority: High

2. Implement Health Endpoints (if not already deployed)
   - Problem: monitoring endpoints required for smoke tests and orchestration.
   - Next steps:
     - Add `GET /health` and `GET /api/health` to `/backend/main_server.js` (return DB+Redis status).
     - Add `/api/serpents/health` to `/backend/serpents-service/index.js` (check Redis and service config).
     - Test with: `curl -f http://localhost:3002/health`.
   - Priority: High

3. Document Env Vars (`.env.template`)
   - Problem: missing documented required env vars for dev and production.
   - Next steps:
     - Create or update `/backend/.env.template` with required vars (DB, JOOMLA_DB, REDIS_URL, API keys, JWT_SECRET).
     - Add notes for typical dev values and security warnings.
   - Priority: Medium

4. Implement Bcrypt for New Registrations
   - Problem: new registrations still use MD5:salt; must use bcrypt for Joomla compatibility.
   - Next steps:
     - Update `joomla_password.js::hashPassword()` to use bcrypt and convert `$2b$` to `$2y$` if needed.
     - Update registration endpoint in `main_server.js` to call new hash function.
     - Run local registration smoke test.
   - Priority: Medium

5. Create Single `requireAuth` Middleware
   - Problem: token/session validation duplicated in many places.
   - Next steps:
     - Implement `/backend/middleware/auth.middleware.js` with unified checks (header, cookie, query param) and call `verifySession()`.
     - Replace duplicates in `main_server.js` with middleware.
   - Priority: Medium

6. Harden Event Discovery Fallback
   - Problem: inconsistent schemas and unreliable fallback chain.
   - Next steps:
     - Add `normalizeEvent()` to `/backend/fetcher.js` and schema validation in `/backend/api-event-fetcher.js`.
     - Add retry/exponential backoff in serpents-service where appropriate.
     - Add tests for sources returning <5 events to ensure AI fallback triggers correctly.
   - Priority: High

7. Prepare Docker-Compose + Nginx (production-ready compose)
   - Problem: need a production compose + reverse proxy for deployment and smoke tests.
   - Next steps:
     - Create `/docker/nginx/nginx.conf`, `/docker/nginx/Dockerfile` and `docker-compose.prod.yml` (minimal: db, redis, node, nginx, optional joomla, dotnet).
     - Add health check-ready compose service names to smoke test commands.
   - Priority: Medium

8. Stabilize BullMQ Worker
   - Problem: need to test Redis connectivity, job enqueue/processing and retry behavior.
   - Next steps:
     - Run Redis connectivity checks and enqueue a job; verify worker logs and serpent_jobs table.
     - Add monitoring commands to `docker-compose` and health endpoints.
   - Priority: Medium

9. Run Integration Smoke Tests (pending)
   - Problem: smoke tests not yet executed after changes.
   - Next steps:
     - From `/var/www/KEEP-Up/docker` run:

```bash
docker-compose up -d db redis node nginx
curl -f http://localhost:3002/health
curl -f "http://localhost:3002/api/events/discover?q=Porto%20Alegre"
```

   - Capture logs if tests fail: `docker-compose logs --tail=200`.
   - Priority: High

---

Notes:
- I removed large local Ollama blobs and Wine prefixes earlier today; those cleanups are complete and not listed here.
- If you'd like, I can start with implementing health endpoints and then run the smoke tests to validate the system.

---

Generated: 2026-01-23T01:54:00Z
